# Spec — Gap 批次（去评分化文案 + 主题活动卡 + KTV 强弱 + 清理）

> 2026-06-15 · 对应 [Gap TODO](../dev-memory/TODO.md) 🟢 档 + KTV 取舍 · 文件：`Home.tsx`、`MyPage.tsx`、`PracticeRoom.tsx`、`index.css` · 状态：实现中

## 修改建议
报告里标 🟡 的纯前端项一次性做掉（不依赖后台），并按用户定调处理 KTV。

## 解决思路 / 技术方案
1. **Home.tsx**
   - streak（P2-1）：`Day 5 streak · keep going!` → `5 practices this week`（去 streak 压力，软化为练习证据）。
   - 92 pts（P2-2）：Last Highlight 的 `92 pts` 去掉裸分，改为证据标签（如 `Vocabulary win` + `phrase saved`）。
   - CTA（P2-3）：主按钮 `Start Speaking` → `Start Practice`。
   - 主题活动卡（#5/P5）：新增一个 themed events 区，4 张卡：科普英语 / TOEFL / 旅游 / 日常对话，点击带 topic 进 `/practice`。
2. **MyPage.tsx**
   - `Ability portrait` → `Practice growth`（P6-3）。
   - `User management` → `Settings`（P7-2）。
   - Coach mode 值 `Gentle prompts` → `Practice feedback only`（P7-3）。
3. **coach-mode 信任标签推广**（P1/跨页）：在 PracticeRoom 顶部加一枚小 "practice feedback only" 信任 chip；My 头部也点明（现仅 Takeaway 有）。
4. **PracticeRoom KTV 强弱**（P3-2，用户定调"保留显示"）：四指标常显作为鼓励框架，但
   - `ktvScore[key] === 0`（还没得分）→ 弱化：整行降透明度、图标/数字灰色、进度条灰。
   - `> 0`（已得分）→ 正常专属色。
   - `activeKtvMetric === key`（刚加分）→ 维持高亮（amber 描边）。
5. **清理**（🟢）：删 `recentLines`（无调用）；删 `index.css` 无用类 `practice-coach-strip / practice-bubble-stack / practice-listening-bubble*`。

## 未做（另行决定）
- ⏸ Next Run Plan 置顶 hero（P4-3，用户先放着）。
- 说话中"隐藏"细节指标这条不采纳——用户定调保留显示 + 强弱区分。
- Trying Point 全 app 推广：作为语言原则后续贯彻，本批不强行加组件。

## 验证测试
tsc + build；手动：首页文案/CTA/主题卡；My 改名与文案；练习页 KTV 未得分项变灰、得分项上色、加分项高亮。
