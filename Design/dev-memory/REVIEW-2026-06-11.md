# 审阅清单 · 2026-06-11 练习页"说话板块"重设计（A/B/C/D）

> tsc+build 通过，**未浏览器验证**。先 **Cmd+Shift+R 硬刷新** `#/practice`（Chrome/Edge+麦克风）。
> Spec：[2026-06-11-speaking-panel](../specs/2026-06-11-speaking-panel.md)。⚠️ = 需你拍板。

| 项 | 改动 | 🧪 验证点 | ⚠️ 关键 |
|---|------|-----------|---------|
| A 字幕 | 5 行提词器：`recentLines` + `.practice-transcript-reel` 固定 8.5rem + 上下渐隐 | 连续说→字幕**固定高度不再越说越长**；当前句**居中加粗**、旧句上滚淡出 | ⚠️ 按"句"切行（句号/?!），没有标点的长串会整段当一行（截末 14 词）。若想按视觉行切再说 |
| B AI 实时 | 预生成缓冲：每段 chunk 的 Gemini follow_up 存入 `bufferedReplyRef`；停顿直接弹（0 等待，真 AI） | 说一段后停 3s→**立刻**弹出基于刚说内容的问题/提示（不再等 1-3s、不再套话） | ⚠️ 弹的是"上一段"生成的候选，可能比"最后半句"略旧；第一次停顿（还没出过 chunk）会用本地 |
| C 触发 | 停顿阈值 4.8s → **3s** | 停约 3s 就给反馈 | — |
| D 布局 | 补全 `practice-*` CSS：猫居中、反馈合并为一张 `practice-prompt-card`、隐掉冗余状态行 | 版面=猫(居中)+5行字幕+一张卡，明显更干净 | ⚠️ 猫旁的次要状态行已用 CSS 隐掉；若想留可恢复 |
| - | 接手了上次未提交的 JSX 重构（cat-stage/bubble-stack 等）+ 补 CSS | 整体不再"没样式" | ⚠️ `resolvePauseReply`/`fetchFollowUpReply` 现已不调用（保留未删），后续可清理 |

**白屏修复**：`lastLine` 改名 `recentLines` 时漏改调用→已全部替换为提词器；另加了 App 级 ErrorBoundary（以后崩溃会显示红字而非白屏）。

---

## v2 微调（同日体验反馈，已实现待验）

| 项 | 改动 | 🧪 验证点 | ⚠️ |
|---|------|-----------|----|
| 字幕连续 | 改回连续记录（不再按句断行）；最新行自动居中、旧文上滚、上下渐隐；高度 7rem 略缩 | 连续说→记录**连贯不跳行**；正在说的那行**锁中间**、往上滚 | ⚠️ 居中靠 padding+autoscroll，不同字号下中点可能差几像素，可调 padding(2.75rem) |
| 状态合并 | 猫旁两层→一层（dot + 一行 coach 文案），删 label/soft/tiny | 猫下只剩**一行**状态 | — |
| 反馈更快 | 停顿阈值 3000→**2500ms** | 停约 2.5s 就弹 | ⚠️ 太灵敏会"抢话"就调回 3000 |
| Navigator 引导 | 新增 `PracticeOnboarding`：首次进练习页弹 4 步讲解（KTV/猫/字幕/开始）+ Skip/Next，只出一次 | 首次进 `#/practice` 弹引导；点完不再出。**重置**：DevTools→Application→Local Storage 删 `speakspark.practiceOnboarded` | ⚠️ 步骤是"卡片序列"非锚定到具体元素；要不要做成指向各板块的高亮气泡再说 |

> 备注：`recentLines` / `resolvePauseReply` / `fetchFollowUpReply` 现已无调用（保留未删，后续清理）。

---

## v3 微调（Navigator 锚定 + 图标）

| 项 | 改动 | 🧪 验证点 | ⚠️ |
|---|------|-----------|----|
| Navigator 锚定 | 引导从"居中卡片序列"改为**锚定到具体板块的高亮气泡**（spotlight 挖洞 + 绿框 + 气泡）：3 步指向 KTV / 猫 / Start | 首次进 `#/practice`→暗背景，依次高亮 **KTV条→猫→Start 按钮** + 旁边气泡解释；Skip/Next；只出一次 | ⚠️ spotlight 靠 `getBoundingClientRect`，元素位置变动需 resize 才重测；步骤指向"未开始"时可见的板块（字幕在 started 后才有，故用 Start 按钮代讲） |
| 删常驻提示 | 删掉 KTV 下"Scores move when you keep speaking…"那句（已由引导解释） | KTV 下不再常驻那行字 | — |
| 图标换新 | KTV emoji（🌊📚🧩🧠）→ lucide 线性图标（Waves/BookOpen/Blocks/Brain） | KTV 四项 + "+N"事件图标变成清爽线性图标 | ⚠️ TakeawayPage 的 KTV 图标是另一份 KTV_META（仍 emoji），要统一的话也改那边 |

---

## v4 微调（练习页清理）

| 项 | 改动 | 🧪 | ⚠️ |
|---|------|----|----|
| 删 AI moments | 顶部左上「⭐ N AI moments」删掉（一半是用户自己点的高光数，易混淆）→ 换成 **🎯 topic/mode** 提示放最上面 | 顶部左上是话题/模式、不再有 AI moments | — |
| KTV 图标上色 | 四个图标 + 进度条 + 数字用**各自专属色**（Flow蓝/Words绿/Sentences橙/Story紫），不再灰 | 不再"像没启动" | — |
| KTV 含义显示全 | 去掉 `truncate`、列宽 56→68px，purpose 改成完整短语（Keep talking / Strong phrases / Fuller sentences / Complete the point） | 每项含义完整可读 | — |
| 删空事件框 | KTV 下无涨分事件时**不再占一个空框** | 没事件时不留空框 | — |
| transcript 可见+铺满 | transcript-shell 改 `flex-1` 居中→吸收空白、**不再被 panel 裁掉**；面板填满视口；reel 7→7.5rem | transcript 始终可见、当前行居中；页面铺满不留底部空白 | ⚠️ 短屏 + 反馈卡很高时仍可能挤；真机看下 |
| 顶部去重 | 面板头里重复的 mode/topic 行删掉（已上提顶部栏） | 头部更干净 | — |

**未做（下一步）**：Home 首页的 Navigator 引导（你提到的"再把 Navigator 首页加上去"）—— 单独做，等你确认。

---

## v5 微调（截图反馈：去重 + 固定槽位 + 暂停可靠性）

| 项 | 改动 | 🧪 | ⚠️ |
|---|------|----|----|
| 去重 coach 文案 | 删面板头"Coach is following your story / I'm with your idea"；猫的并排状态气泡也删——状态只在猫下方**一个槽位**显示 | 不再有两三处重复的 listening 文案 | — |
| 固定槽位 | 猫下方新增 `.practice-coach-slot`（固定 118px）：平时显示一行状态；停顿出问题时**同位**换成卡片，尺寸固定**不跳动** | 状态↔卡片切换时下方 transcript 不位移 | — |
| 暂停卡片不出来 | 猜测根因=`ENERGY_THRESHOLD 0.003` 太低、环境噪音被当说话→静默到不了阈值。调 **0.003→0.006**；并在 Use it/Skip 后 reset `pauseHandledRef` 让下次暂停能再触发 | 停顿 ~2.5s 应弹出问题卡 | ⚠️ 若仍不出/太灵敏，这个阈值要按你的麦克风再调；这是启发式，没法在我这边实测 |
| 压缩下方 | 外层 gap-3→2、pb-28→24、Save 按钮变矮 | 字幕下方更紧凑 | — |
| 猫只画猫 | AICharacter 去掉气泡栈，只渲染猫 | 猫更聚焦居中 | 备注：`practice-coach-strip/bubble-stack/listening-bubble*` CSS 现已无用 |
