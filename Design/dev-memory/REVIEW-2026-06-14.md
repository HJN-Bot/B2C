# 审阅清单 · 2026-06-14 Takeaway 重构为 3 大块（长页）

> tsc + build 通过，**未浏览器验证**。先练一次再看 `#/session-end`（路由要带 `#`）。
> Spec：[2026-06-11-takeaway-3blocks](../specs/2026-06-11-takeaway-3blocks.md)。⚠️ = 需你拍板。

文件：`src/pages/TakeawayPage.tsx`（仅此一个文件改动）。

| 块 | 内容 | 🧪 验证点 | ⚠️ |
|---|------|-----------|----|
| **① Your run** | 带数字徽标的大标题 → Hero(🎉+headline+词数) + **AI encouragement** 暖句(新露出) + coach-mode pill + What you talked about + **What you did well**(含 "You said:" 引用，从原底部二列移上来) + Your progress this run(KTV 分值+What moved) | 从上到下：庆祝→鼓励→讲了什么→做得好(带引用)→四项分值 | encouragement 之前只在 chat 首条，现在 hero 下也显示一句 |
| **② Level up** | 大标题 → **Amplify·do more of this**(绿，**新渲染** `amplify` 字段，带引用) + **Change·one thing to fix**(橙，=make_stronger，带引用) + **Next time, say it like this**(原 Next Run Plan：focus/say_this/reuse_words/one_move) | 放大→改掉→下一次怎么说，三段清晰 | ⚠️ amplify 是 `ae1032b` 加的 schema 字段，本次才上墙；旧 session 缓存里没有该字段时走 normalize 兜底文案("Keep developing your strongest idea further.") |
| **③ Coach** | 大标题 → Coach chatbox(多轮+presets) + Practice Again / Start | 追问框 + 两个按钮 | 删了原 "④ Go again" 小标题，并入块③ |

**附带（🟢 收尾项）**
- KTV 图标 **emoji → lucide 上色**：TakeawayPage 的 `KTV_META` 由 🌊📚🧩🧠 换成 Waves/BookOpen/Blocks/Brain，并按各自专属色（Flow蓝 #58A9FF / Words绿 #16A34A / Sentences橙 #D97706 / Story紫 #7C3AED）渲染图标+进度条+数字 → 与练习页 KTV 配色统一。
- 进度条原来一律绿色，现改为按指标专属色。

**结构对照（旧 → 新）**
- 旧：Hero → pill → talked-about → ②进步 → ③Next Run Plan → What worked/Make stronger 二列 → Chatbox → ④Go again
- 新：①(Hero+鼓励+talked-about+**What you did well**+进步) → ②(**Amplify**+Change+Next Run Plan) → ③(Chatbox+Go again)
- 即：what_worked 上提进块①、make_stronger 进块②、新增 amplify、Next Run Plan 下移进块②。

**未动**：prompt/normalize/local 兜底（`ae1032b` 已就绪）；后台无需重新部署（纯前端渲染重排）。
